{
  "name": "pineywoods-realty",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "node-sass": "^4.5.3",
    "node-sass-chokidar": "^0.0.3",
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-router-dom": "^4.2.2",
    "react-scripts": "1.0.14",
    "redux": "^3.7.2",
    "sass-loader": "^6.0.6",
    "video-react": "^0.8.8"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "build-css": "node-sass-chokidar src/ -o src/",
    "watch": "npm run build-css && node-sass-chokidar src/ -o src/ --watch --recursive"
  }
}
